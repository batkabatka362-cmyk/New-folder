# memebot — Autonomous Build Roadmap

The bot self-advances this list (via `/loop`). Each iteration: pick the next
`[ ]` item in the **Autonomous queue**, implement it, verify (compile + tests +
live smoke where applicable), adversarially review when substantial, then check
it off and continue.

Legend: `[ ]` todo · `[~]` in progress · `[x]` done · `[BLOCKED: …]`

## Done
- [x] Phase 1 — data ingest + paper executor (stacked fees) + risk + SQLite (reviewed, 14 fixes)
- [x] Phase 3 — autonomous brain: constitution, Verdict/Reflection, memory, reflection (reviewed, 7 fixes)
- [x] Local AI — Ollama backend (gemma3:4b live), `auto` prefers local (reviewed, 7 fixes)
- [x] Phase 2 plumbing — features, GBM scorer (dormant), backtest label/train/replay (reviewed, 7 fixes)

## Autonomous queue (SAFE: local code, tests, offline tooling only)
- [x] R1 — Test suite: dependency-free runner + pytest-compatible tests over core logic (27 tests, all pass; `python run_tests.py`)
- [x] R2 — On-chain safety: top-5 holder concentration via Helius RPC (getTokenSupply + getTokenLargestAccounts, vault-excluded), wired for MIGRATED tokens; activates the previously-dead concentration gate. (LP-burn deferred → needs pool/LP-mint discovery, Phase-4/Bitquery.)
- [x] R3 — Telegram commands (/pnl /positions /status /stop /resume /help) via getUpdates long-poll (httpx, no python-telegram-bot); chat-id restricted; pure render_command unit-tested. RiskManager.resume() added.
- [x] R4 — Parquet archival (storage/archive.py): snapshot export of candidates/trades/equity/lessons/verdicts; periodic loop + CLI (`python -m memebot.storage.archive`); graceful skip if pyarrow missing.
- [x] R5 — Trade-stream watchlist plumbing (feed/watchlist.py): bounded top-N subscribe/unsubscribe diff; eval uses real tick breadth (unique buyers, buy/sell) when trades flow. OFF by default (TRADE_STREAM_ENABLED=false) — the stream is METERED/spends SOL, so never auto-enabled; user opts in with a funded PumpPortal key.
- [x] R6 — Backtest sim (backtest/simulate.py): replay logged candidates (rebuilt from stored features) through rule gate + scorer + entry threshold; net return from forward DexScreener price minus round-trip fees; win-rate & PnL by mode. CLI `python -m memebot.backtest.simulate`. (Hold-to-now approx — no intrabar TP/SL path.)
- [x] R7 — CLI status dashboard (memebot/status.py, `python -m memebot.status`): latest equity/ROI, candidate->buy/sell funnel + exit-reason breakdown, recent verdicts, learned lessons. Read-only (WAL-safe); pure render_summary unit-tested.
- [x] R8 — Hardening: Settings.validate() (startup config warnings), graceful-degradation audit (all degradations confirmed: no-key→rule, no-pyarrow→skip, no-lightgbm→rule, Ollama-down→None, feeds-down→skip), expanded edge tests (config/balance/evict/candles/indicators/token-state), README synced to current state.
- [~] R9 — GBM activation: deps installed ✅; collector gathered 12k+ candidates / 273 usable mints. First training run (171 labeled, +30% bar): **AUC 0.53, only 4% positives → NO real edge yet.** Correctly NOT deployed (renamed gbm_model.weak.txt; rule scorer stays — a near-random model would be worse than transparent rules). Needs: more data, longer/consistent forward horizon, tick features (trade stream), and class-imbalance handling. Collector keeps running; retrain later. dataset.csv kept.

### User strategy additions (paper)
- [x] R10 — Compounding sizing: RiskManager.position_size_sol now = equity × risk_per_trade_frac × brain size_pct, capped by max_position (2.0) + 95% balance + liquidity-fraction. Grows with equity; unit-tested.
- [x] R11 — Breakeven stop: Position arms once up ≥breakeven_arm_pct (12%), then exits at "breakeven" if it falls back to ≤breakeven_floor_pct (2%). Protects winners from round-tripping to a loss; unit-tested (both modes, un-armed still uses SL).
- [x] R12 — Min expected-R entry filter: _try_open skips a buy unless expected_return(conviction, tp, sl, round_trip_cost) ≥ MIN_EXPECTED_RETURN. round_trip_cost_pct moved to utils/money (shared with backtest). Unit-tested.
- [x] R13 — Dynamic holder-driven re-eval: _reeval_loop re-checks OPEN positions' concentration every reeval_interval_s — `cut` (≥holder_cut_pct → exit fast), `extend` (≤holder_good_pct → scalp→hold), `hold`. Sell path refactored into _close_position (shared by manage + reeval). Unit-tested.
- [x] R14 — Local-AI comprehension (user-requested): demo showed gemma3:4b mis-reading "revoked authority" as bad. Fixed via prompt clarity — constitution now states the SAFE/DANGER polarity explicitly + few-shot examples; candidate prompt labels each authority "REVOKED -> SAFE" / "ACTIVE -> DANGER"; reflect prompt demands a full-sentence lesson. Verified live: gemma now reads safety correctly (skips only for valid reasons like unknown concentration) and writes full-sentence lessons. Deeper reasoning still limited by the 4B model — qwen3:8b / cloud Claude reason better.

### Strategy tuning (paper, data-driven)
- [x] T1 — Entry-threshold sweep tool (`memebot.backtest.sweep`): backtests a grid of thresholds over collected candidates, reports entries/win%/avg-net/total-net per threshold, picks the best-net. Reusable as data grows; pure `threshold_grid` unit-tested.
  - First sweep (298 mints, rule scorer, 9.5% round-trip): <0.50 enters indiscriminately and bleeds (−20% avg/trade); ≥0.50 turns selective + net-positive; 0.55 was too strict (1 trade). → tuned **ENTRY_THRESHOLD 0.55 → 0.50**.
  - ⚠️ Caveat: thin sample (13 entries at 0.50), fat-tailed (a few moonshots dominate), and hold-to-now overstates winner magnitude (live exits at TP/breakeven sooner). The robust takeaway is "be selective," not the headline +%. Re-run the sweep as the collector accrues more data.
  - 🐛 Found+fixed during tuning: `Settings.load()` had a SECOND hardcoded `ENTRY_THRESHOLD` default (0.55) that silently overrode the dataclass default — config changes weren't reaching the running bot. Synced to 0.50 and added an audit (Settings() vs Settings.load() defaults now verified identical for every field). Demo confirms band = entry 0.5.

### Phase D — Defensive risk posture (user chose (a) defensive; delegated data approach (b) to me)
Sim evidence reframes "defensive": for memecoins, capital preservation comes from **size / exposure / selectivity**, NOT tighter stops (a tight SL whipsaws winners and can't stop an instant rug). Tune accordingly.
- [x] T2 — Synthetic-scenario exit simulator (`memebot.backtest.scenarios`): runs canonical price paths (moon / pump_dump / slow_bleed / chop / rug / quick_scalp / fakeout_recover) through `Position.should_exit`; `--compare` shows current vs a defensive profile side-by-side with worst-case. **Found+fixed a real latent bug:** `should_exit` used `now or time.time()`, treating an explicit `now=0.0` as unset. Unit-tested. Finding: tight SL whipsaws (fakeout: −21% vs +40%) and does NOT shrink the rug worst-case (−79.5% either way).
- [x] D1 — Wired `ExitParams` into `Settings` as a nested `exit:` block (env-overridable via SCALP_TP_PCT/…/BREAKEVEN_*; was hardcoded `ExitParams()` in main.py → now `settings.exit`). Added `RiskLimits.max_total_exposure_frac`. validate() guards the new fields; defaults-audit (F2) confirms load()==dataclass. No import cycle (config→portfolio→stdlib).
- [x] D2 — Applied the **multi-agent-designed defensive profile** (workflow wbqnrfg25: 4 proposals → 4 adversarial critiques — all flagged the proposals "too conservative" → 1 synthesis that corrected them). Final: entry 0.50→**0.55**, min_EV 0.0→**0.03**, risk_per_trade_frac 0.05→**0.04**, max_position_sol 2.0→**1.0**, max_positions 8→**3** (enforced ✓), daily_loss_cap 2.0→**1.5**, breakeven arm 0.12→**0.20** / floor 0.02→**0.10** (fixes the "breakeven exits at −7.5% net" hidden loss). Deliberately NOT changed: scalp_tp stays 0.40 (cutting to 0.30 inverts R:R to negative-EV per the synthesizer's math), scalp_sl/hold_sl/trail stay (tightening whipsaws). `scenarios --compare`: default avg +17.5% / worst −79.5% beats over-tightened +4.8% / −79.5% (same worst — tightening doesn't help rugs). 59 tests green; demo enters at 0.04 sizing (0.32 SOL). NOTE: `max_total_exposure_frac=0.15` is config-only so far — **enforcement is D3**.
- [x] D3 — Exposure guardrails (risk-layer enforced, unit-tested): (a) `max_open_positions`=3 already enforced in can_open; (b) **`max_total_exposure_frac` now ENFORCED** — `position_size_sol` caps a new buy by `equity×frac − Σ open cost_sol`, so aggregate deployed capital can't exceed 15% of equity (fixes the review's "dead config" finding; size→0 ⇒ buy skipped); (c) **consecutive-loss circuit breaker** — `max_consecutive_losses`=5 (0=off) trips a `loss_streak` halt via `on_loss`, lifted by a winning close (`on_win`) or UTC-day rollover. 63 tests green; demo OK.
  - 🔎 **Adversarial review** (2 focused subagents over D1/D2 + the now-fixes): no regressions; import-cycle-free; defaults-audit holds; the now-fix confirmed to repair the scenarios scalp-timeout corruption. Two findings → both resolved: (1) `max_total_exposure_frac` was dead config → enforced in D3; (2) breakeven could mislabel a loss as "breakeven" → now `should_exit` returns "sl" when an armed give-back lands below entry (honest reflection signal), unit-tested.
- [x] D4 — **EV-margin entry sweep** (DONE): `simulate()` gained `min_expected_return` (None=no gate, backward-compat; a numeric value — incl. 0.0/negative — applies the LIVE gate `expected_return(score,…) >= buffer` using the score as the conviction proxy, matching `Bot._try_open` which always gates). Helper `_ev_tp_sl` mirrors the live HOLD trail-cap (never the unrealizable +300% ceiling). New `backtest/evsweep.py` sweeps the buffer at a fixed threshold and picks the best AVG-net buffer via the pure, unit-tested `pick_best_ev` (requires ≥min_entries so one lucky trade can't crown a buffer). CLI `--min-ev`. Unit-tested: mixed pass/skip across a between-EVs buffer, `_ev_tp_sl` both branches, `pick_best_ev` min-entries floor.
- [x] D5 — **Sizing / survival review** (DONE): added **`max_position_equity_frac`=0.05 — a HARD per-trade ceiling** (one position's SOL = its full rug loss ≤ this fraction of equity), enforced as an independent cap in `position_size_sol`. It's a backstop ABOVE `risk_per_trade_frac`=0.04 (so it doesn't bite normal sizing) that holds even if `frac`/`target` assumptions change AND **tightens automatically as equity is drawn down** (unlike the absolute `max_position_sol`). `validate()` warns if `risk_per_trade_frac > max_position_equity_frac`. New `RiskManager.sol_at_risk(equity)` quantifies per-trade + whole-book worst-case rug vs the daily cap, **logged at startup**. **Survival finding (honest):** with the defaults the whole-book worst case = `min(per_trade×3, 15%·equity)` and the 15%-exposure term crosses the absolute `daily_loss_cap_sol`=1.5 at **exactly equity=10** — so `book_within_daily_cap` is False (no headroom) at/above the initial equity. The equity-relative caps (5% per-trade / 15% aggregate) ARE the survival invariants; the absolute daily cap is a fixed secondary breaker that becomes a tighter *relative* trigger as equity grows. A future option (NOT taken without user input): make `daily_loss_cap_sol` equity-relative. 100 tests; **5-dimension adversarial-review workflow → 8 confirmed findings fixed** (None-sentinel for negative/zero min_ev = live parity, strict `<` to match the breaker's `>=` trip, softened the snapshot-bound docstring, + 4 test-coverage gaps closed: mixed EV, `_ev_tp_sl` hold branch, `sol_at_risk(0)`, book-cap False branch).

### Performance snapshot — first 22.6h live paper run (OLD pre-defensive config)
- equity **+0.9%** (+0.0876 SOL) from only **2 round trips**: one **+106%** (trail exit), one **−36%**. n=2 ⇒ statistically meaningless — cannot judge edge yet.
- **Funnel bottleneck found:** the rule scorer **maxes at 0.56** across 27k snapshots (clustered ~[0.40,0.50]; cliff: 0.50→2261 vs 0.55→4). So `entry_threshold=0.55 ≈ the ceiling` → only **2 of 2250 mints** ever qualified → the bot barely traded. And `high_conf=0.8` (slam-dunk tier) is **unreachable = dead code**.
- Immediate fix applied: `entry_threshold` 0.55 → **0.50** (admits 128 mints, top ~6%). Capital preservation stays in SIZE/exposure/breakeven/daily-cap — NOT in starving entries. (User can set ENTRY_THRESHOLD=0.55 to go ultra-selective.)
- [x] D6 — **Scorer calibration** (workflow wu9m8gdl0 → 4-agent diagnose+design, then implemented). Root cause: a `social=0.5` placeholder + HOLD `safety_quality` capped at 0.7 + `_norm` saturating only at 3–5× the gate min → raw rule sum stuck in ~[0.3,0.56]. Fix in `signals/scoring.py`: dropped the dead social term (weights → SCALP 0.78/0.22, HOLD 0.45/0.40/0.15), let known-safe HOLD safety reach 1.0, lowered two normalizers to 2× the gate, then **affine-rescale `[ref_lo=0.30, ref_hi=0.62] → [0,1]`** (monotonic, rank-preserving; new config fields). Made high_conf scorer-kind-aware: `rule_high_conf_threshold=0.85` (rule scale) vs `high_conf_threshold=0.80` (GBM prob) — resolved in main, threaded through `TradingBrain`→`Escalator`. validate() guards the new fields.
  - 📊 Re-scored the 29,873 collected snapshots offline: scores now span **[0,1.0]** (was max 0.56). Entry 0.50 admits **375 mints** (was 2 at the broken 0.55); slam-dunk 0.85 reached by **11 mints** (rare, as intended). The 3-tier decision (skip / LLM-band / slam-dunk) now functions, and the conviction→size signal is restored (was pinned at the 0.25 floor).
  - 🔎 2-subagent review: **no bugs** — rescale monotonic & bounded, GBM path untouched, defaults-audit holds, and a size_pct=1.0 verdict still opens only ~0.4 SOL (the defensive caps + min_EV + can_open all bind after the verdict). 66 tests green; demo lands in-band (0.79) so the LLM is still showcased. (Honest caveat per the design dissent: AUC≈0.53 means this is a better-scaled *rank*, not proven edge; rule_high_conf kept high at 0.85 so act-local stays rare. ref_hi=0.62 fit to one sample → re-derive from a rolling window.)

### Phase AI — "AI trader, NOT a bot" (user steer: self-reasoning / learning / trend-aware, not just rules)
The mechanical layers (rule scorer, thresholds, risk caps) are necessary but they're a *bot*. This phase makes the AI the reasoning core — reading price action / trendlines, aware of its own performance, and learning to adapt. **Prioritized over the remaining quant items (D4/D5/E).**
- [x] AI1 — **Price-action / trendline context in the brain prompt.** Was a snapshot-only reasoner; now the AI reads the TRAJECTORY: DexScreener 5m/1h % change (were fetched then discarded → now on the Candidate) + a short-term trend from a new per-token polled price history (`TokenState.price_obs` + `record_price` + `price_trend`: direction / % change / % off recent high). Prompt gained a "PRICE ACTION / TREND" block + guidance ("a rising/steady trend favors entry; buying a sharp drop far below the high is catching a falling knife"). Verified live: gemma now reasons e.g. *"5m and 1h price action demonstrate significant upward momentum, favorable for a scalp"* and weighs it against safety. 68 tests; trend/price_change deliberately NOT in FEATURE_NAMES so the scorer/GBM/archival are untouched.
- [x] AI-doctrine — **Encoded the user's operating doctrine into the constitution** (policy.py): SURVIVE first (break-even exit over any loss; never let a winner round-trip to a loss); be DECISIVE/FAST (book a defined profit, don't wait for the top); HOLD only when durable safety converges (LP burned/locked + mint&freeze revoked + healthy holders) else SCALP-and-exit-fast watching the flow; COMPOUND but never at the expense of survival. Demo proof: gemma flipped from SKIP → **scalp BUY with a tight exit** on a rising + revoked token, reflecting "swift tight scalp exit to mitigate risk."
- 🔴 **Launch-catch reality (verified on 39k candidates / 5 trades):** we do NOT catch coins at the launch moment. 99.6% of candidates are brand-new bonding-curve tokens with **no real-time data** (no DexScreener price/vol/trades yet), so they can't be evaluated; ALL 5 paper trades were DexScreener-indexed HOLD tokens ($15–20k liq, i.e. already matured). True launch-moment trading needs the **metered PumpPortal trade stream** (real-time curve trades) — that **spends SOL = G3, gated, awaits explicit user approval** (I will NOT auto-enable it). On the free tier we trade tokens once a DEX pool indexes them (later, safer, but not "the launch second").
- [ ] AI-live — **Demo-test the new code on REAL coins**: restart the paper bot (it's running on pre-D6/pre-AI code) so the calibration + doctrine + trend-context go live, then observe over a window how many real coins it now catches/paper-trades and the catch→exit outcomes. (Paper mode, free feeds only; stop the old `python -m memebot` first so there's one WAL writer on memebot.db.)
- [x] AI2 — **Self-awareness**: the candidate prompt now carries a "YOUR BOOK" block — equity vs start, open/max positions, today's loss vs daily cap, loss streak, recent win-rate (from `Bot._account_state()`, threaded into `brain.decide(c, self_state)`), with guidance to be more selective / skip near the cap or on a streak. `RiskManager.consecutive_losses` exposed. Backward-compatible (no state → no block). 72 tests; unit-tested.
- [x] AI3 — **Meta-learning loop**: `_meta_reflect_loop` (every `meta_reflect_interval_s`=1800s, needs ≥5 closed trades) calls `brain.meta_reflect(stats, recent)` — an AGGREGATE review (win-rate, avg/total pnl, exit-reason breakdown) that distills ONE strategy-level lesson, stored (tag defaulting to "global", confidence-boosted) so it's recalled for future decisions. Advisory only — never edits config; the risk layer still vetoes. Self-adapting via memory. 73 tests; unit-tested (fake-LLM meta-reflect stores + recalls).
- [x] AI4 — **Deeper trend/technical signals**: `TokenState.technicals()` computes EMA-cross (fast vs strictly-slower slow), breakout/breakdown vs the recent range, and % distance to support/resistance from the polled `price_obs` series (reuses `indicators.ema`); fed to the prompt as an "indicators:" line in the PRICE ACTION block. Guarded (n<5 warmup, div-by-zero). 74 tests; unit-tested (uptrend → EMA bull + breakout up). Adversarial review of AI2+AI3+AI4: no crash/regression bugs; EMA equal-period edge at n=5 fixed.
- [x] AI5 — **Winner-characteristics study** (`python -m memebot.backtest.winners`, user-requested): on the 5m labeled data (83 winners / 337 losers, +50%/5m), compares winner-vs-loser feature medians. **FINDINGS: 1-hour VOLUME is the dominant tell — `vol_h1` winner-median 2247 vs loser 1096 (lift 2.05×); then `market_cap_usd` (1.39×) and `vol_to_mcap_pct` (1.39×). `unique_buyers` weak (1.08×); `buy_sell_ratio` does NOT separate (1.02×); `liquidity_usd` median 0 for both (a data nuance — many indexed tokens log 0 liquidity, so AI9's liquidity>0 gate may be over-strict — revisit).** Actionable: the scorer/classifier should weight VOLUME (vol_h1 + vol/mcap + mcap) heavily and stop leaning on buy/sell. Pure `winner_characteristics()` unit-tested.
- [~] AI6 — **PART 1 DONE** (`rules.scam_likelihood(c,s)` → graded 0..1 danger from authority-active / low-LP / high-concentration / unknown, shown in the prompt SAFETY block for the AI to weigh, distinct from the hard veto; unit-tested). PART 2 pending data: retrain the GBM. Original: **Scam-detection "sixth sense" + data-grounded intuition** (long-term, the AGI-like goal): a small set of high-signal scam checks (authority / LP / holder-concentration / creator-dump patterns) producing a fast "scam-likelihood" the AI weighs, PLUS retrain the dormant GBM (R9) once the trade stream yields enough labeled curve-phase data (the 171-sample AUC-0.53 attempt failed for lack of data) — so judgments rest on learned data, not just hand rules. Pairs with AI3 (the learning loop). Raises the survival rate, which is the prime directive.
- [x] AI7 — **Holder-quality → hold-vs-scalp** (user doctrine): `rules.holder_quality(c,s)` → healthy (conc ≤ holder_good_pct) / concentrated (≥ holder_cut_pct) / mixed / unknown, shown in the prompt ("healthy => can HOLD; concentrated => SCALP and exit fast") so the brain leans hold-vs-scalp on holder health at ENTRY (complements R13's in-flight re-eval). 77 tests; unit-tested. (Concentration is Helius/post-migration; deeper holder-count/dev-share growth needs more on-chain calls — future, with G3/Bitquery.)
- [x] AI8 — **No blind scalp** (from the live stress test): `Bot._has_momentum(c)` requires a real signal — `price_change_m5/h1 > 0`, OR a rising short-term trend, OR `vol_spike >= 1.5` — before a SCALP buy; `_try_open` SKIPs an otherwise-approved scalp without it (config `require_scalp_momentum`, default on; HOLD exempt). Stops paying the round-trip fee on zero-signal curve entries. 76 tests; unit-tested. (Expect this to cut free-tier trade count sharply — intended.)

### Phase E — Data pipeline (the (b) I decided): make the collected data actually useful
- [x] E1 — Fixed-horizon labeling: `label.fixed_horizon_labels()` + `--horizon`/`--tol` flags label each mint's earliest obs by its forward return at ~+H from the SAME mint's LOGGED later prices (offline, no network); drops mints with no price near the horizon (never fabricates). Comparable across mints, unlike "price now". Unit-tested.
- [x] E2 — Data-quality report (`python -m memebot.backtest.dataquality`): observations, distinct mints, tracking-duration, and labelable-count + label-balance per horizon, with a `trainable()` verdict. **Key finding: median token is tracked only ~41s; 30m/2h horizons have ~0 data, but the 5-MINUTE horizon has ~400 labelable mints at ~20% positive — both classes.** Unit-tested.
- [x] **AI6-part2 / R9 — GBM TRAINED WITH A REAL EDGE (the breakthrough).** On the 5m fixed-horizon dataset (402 mints, 20% pos): LightGBM **val AUC ≈ 0.72–0.79, stable across forward splits** (0.6/0.7/0.8 cuts), and a permutation/label-shuffle test collapses to ~0.4–0.66 (noise) → the signal is real, not leakage. (The old R9 attempt got 0.53 because it used variable-horizon "now" labels.) Top features: market_cap, buy/sell, vol/mcap, unique_buyers, vol_h1. **Deployed** (`gbm_model.txt`); the live bot now runs `scorer=gbm`. CAVEATS (honest): val is only ~81 rows + survivorship-biased (labelable = survivors), and the model scores ALL candidates at inference (incl. non-survivors) — so confirm on live paper as more data accrues before over-trusting. 79 tests green.
  - ⚖️ **LIVE VERDICT (11h paper run): the GBM edge did NOT transfer — 6/6 losing scalp timeouts.** The survivorship bias was decisive: trained on tokens that survived ≥5m, it can't price the live population (most curve tokens never survive). So the offline AUC 0.75 is real-but-non-transferable. **Reverted** the live scorer to the transparent rule (gbm_model.txt → gbm_model.unproven_5m.txt); kept the GBM + E1–E3 infra ready for when G3 supplies UNBIASED real-time curve data. No fake confidence in an unproven model.
- [x] E3 — **Safe retrain cadence** (`python -m memebot.backtest.retrain`): regenerates the 5m dataset from the live DB, trains a candidate, and DEPLOYS only if its forward-val AUC ≥ max(floor, currently-deployed AUC recorded in `gbm_model.txt.auc`) — `should_deploy()` NEVER ships a worse model. Verified: a fresh candidate (AUC 0.747) was correctly KEPT-not-deployed vs the live 0.789. Re-derive `gbm_entry_threshold` from the val precision sweep alongside. Run on a cadence as data accrues. Unit-tested (deploy gate).

### Phase F — Robustness hardening (born from the dual-default + now-bug footguns)
- [x] F2 — Defaults-consistency audit baked into the suite (`test_load_defaults_match_dataclass_defaults`): strips config env vars, asserts `Settings()` == `Settings.load()` for every (nested) field. Would have caught the ENTRY_THRESHOLD 0.55/0.50 footgun.
- [x] F3 — `x or default` audit: found the same bug class as `should_exit` in `now = now or time.time()` across **risk/limits.py (×2), agent/escalation.py (×2), data/token_state.py (×1)** — all silently replace an explicit `now=0.0` with wall-clock. Fixed all 5 to `now = time.time() if now is None else now`. Other `or {}`/`or 0` hits reviewed = benign (empty→empty / same value).

### Real-time stress test + bug-hunt (2026-05-31, paper, free feeds)
Ran the NEW code live on real coins (paper). RESULT: D6 works live — the bot now **catches & paper-trades curve-phase scalps** (MAN/MetaMask/GAYHOUSE...), which it never did before. **~31-min run: 7 trades, ALL small-loss timeouts, equity −0.77% (−0.077 SOL, ~−0.01 each), 0 crashes, 14 lessons learned.** On the free tier we can't see a curve token's intrabar move, so a scalp just ages out at a fee-loss — the rule scorer has no real edge here (AUC 0.53), and the AI is already learning it ("low-liquidity + rule-fallback entry → exit fast"). Confirms the system is STABLE and SURVIVES (losses tiny), but free-tier curve-scalp profit needs the **AI layer (AI6/GBM) + real-time data (G3)** — not a quick patch. A 4-agent adversarial bug-hunt then found:
- [x] BUG (HIGH, fixed) — junk LLM verdict (NaN/Infinity from `json.loads`) survived `_clamp` as MAX conviction/size → max-size BUY. `_clamp` now fails non-finite to the floor (0). Unit-tested.
- [x] BUG (HIGH, fixed) — a dead/dark open position (no price) was `continue`d in `_manage_loop`, so it was valued at its stale entry price forever (hid the loss from the kill-switch, blocked every exit incl. timeout). Now: evaluate the time-exit against the last-known price each poll, and force-close `"no_price"` after a sustained blackout (`_max_price_misses`).
- [x] BUG (MINOR, fixed) — stale config comment ("scorer maxes ~0.56"); the social-free scorer's raw spans ~[0,0.86]. Verified ref=[0.30,0.62] is still sound (375 mints clear entry 0.50, only 11 hit slam-dunk 0.85); comment corrected.
- [x] BUG (MEDIUM, fixed) — `position_size_sol` aggregate-exposure cap now values open positions at CURRENT price (`Σ qty*price_map.get(...)`, same basis as `equity()`), not cost; `price_map` threaded in from `_try_open`. Backward-compatible (absent price_map → avg-price == cost). Unit-tested (current-value binds; below-cost frees room).
- [~] BUG (MEDIUM) — labeler "absent → rug=0": investigated, **NOT a bug** — `label._load_rows` only loads `price_usd > 0` candidates (DexScreener-indexed at decision time), so a never-indexed young token is never labeled. Absent-now of a previously-priced token = genuine delisting = correct. No change.
- [x] BUG (MINOR, fixed) — `volume_spike` now drops the still-OPEN bucket (`volumes()[:-1]`) so the spike compares the last CLOSED bucket; the direct `volume_spike_ratio` helper/test is unchanged.
- [x] BUG (MINOR, fixed) — day-rollover now uses ONE clock: `now` threaded through `daily_loss`/`_roll_day`; `can_open` passes its `now`. Test stubs updated.
- [ ] BUG (LOW) — when a G3 api-key is set, the `websockets` library's own DEBUG logs can print the key-bearing URL (our `_redact` only covers our log line). Quiet `logging.getLogger("websockets")` to WARNING when a key is in use. (Deferred with G3.)
- [x] BUG (LOW, fixed) — `RiskManager._cooldown` is now pruned of expired entries in `can_open` (no unbounded growth). `profit_factor=inf` left as-is (LOW, non-manifesting — never JSON-serialized; "inf" is informative in the log line).
- [x] BUG (LATENT, FIXED in P3) — `apply_sell` recorded only the final slice's cost/proceeds in `ClosedTrade` on a partial sell. Now accumulates `realized_cost/proceeds/pnl` per slice on the Position and books the FULL round-trip at close (a single full sell stays byte-identical). No longer latent now that P3 wires real partial take-profits. Unit-tested (partials sum to the same total as one full sell).

### Phase S — Survive via good DATA-BACKED setups (user Option 2; design workflow w6ps3jktt, 5 agents)
Reframe: we can't catch launches (free tier) — so trade only the DexScreener-indexed tokens we CAN read, classify GOOD setups, and survive even if late. Verified root cause of the live bleed: the momentum-only score RANKS already-pumped/fading tokens highest right before they dump on a late entry, and thin ghost pools guarantee a fee-loss.
- [x] **P1 — Deterministic GOOD-SETUP classifier** (`signals/setup.py` `classify_setup` → grade good/marginal/bad + quality). Hard vetoes (→ skip regardless of score): thin liquidity (<8k), ghost liq/mcap (<3%), far-off-high (≥35%), post-pump fade (h1≥150% & m5≤0), downtrend, breaking-down (ema bear / breakout down), wash spike (bsr>4 & <5 buyers), unsafe (scam≥0.5 / freeze active). Unknown-trend/thin-breadth → 'marginal' (never 'good'). Wired into the eval loop (drop 'bad' before ranking) + size scaled by quality (marginal risks half). 83 tests; unit-tested. Config-tunable; thresholds are heuristics to re-derive from the live distribution.
- [x] P2 — **Fixed data collection at the source** (the structural survivorship cure). (a) Observation logging is DECOUPLED from the rule gate: `_evaluate_once` now logs an `observations` row for EVERY indexed snapshot (liquidity_usd>0) — gate-FAILERS included — each token logged exactly ONCE at its most-resolved point (failer→pre-safety, which matches what the live scorer sees for it; survivor→post-safety+final-score, so NO train/serve skew). (b) New `observations` + `trade_outcomes` (entry-features→realized PnL) tables (auto-created via `CREATE TABLE IF NOT EXISTS`, non-destructive on old DBs); `Position`/`ClosedTrade` carry `entry_features`+`entry_score`, written on every full close. (c) `label.fixed_horizon_labels` gained `indexed_only` (in-distribution only) + `death_drop` (a WITNESSED ≥X% collapse with no horizon price → honest label 0, never fabricated); `_load_all_obs(source=)` prefers `observations`, falls back to `candidates`. (d) `dataquality` reports `indexed_observations`/`labelable_5m_indexed`/pos+neg counts; `trainable()` prefers the in-distribution count. **Killer finding the moment it shipped:** legacy `labelable_5m=658` was inflated by curve-only rows — the HONEST in-distribution count is **labelable_5m_indexed=5** (1 pos / 4 neg) over 33 indexed mints. That gap IS the survivorship bias, now measured. 91 tests; **2-agent adversarial review** → 5 fixes applied (train/serve-skew log-point, retrain `--population` gate so E3 doesn't silently switch, archiver exports the new tables, entry_features copy, trainable clarity). Live-verified: observations writing with gate-failers captured. ParquetArchiver now exports both tables. **DEFERRED to P4:** the partial-sell `trade_outcomes` PnL would understate a partially-exited trade (latent — no partial sells today; fix bundled with the P3 partial-TP accounting item).
- [x] P3 — **Survival-first exits** (DONE): breakeven arm 0.20→**0.12**; **arm-trail** `trail_after_arm_pct=0.06` (once armed, exit 6% off peak — applies to SCALP too, reason "be_trail"); **loss-aware scalp stall** `scalp_stall_s=75` (flat-or-down past 75s → "stall"); **fade exit** (`Bot._trend_broke` via live `TokenState.price_trend()/technicals()` — confirmed downtrend / breakout-down / EMA-bear while pnl > -breakeven_floor → "fade", gated `exit_use_trend`, never overrides the hard SL). Quality-sizing shipped in P1. validate() guards partial_tp_pct>round-trip & trail<arm. Scenario sim improved: avg net +17.5%→**+20.4%**, win% 50→**57** (be_trail banks give-backs). **PARTIAL TAKE-PROFIT now wired** (`Bot._take_partial` + `Position.should_take_partial`): once up ≥`partial_tp_pct`=0.15 (above the ~9.5% round-trip), sell `partial_tp_frac`=0.5 ONCE (guarded by `partial_taken`), bank the SOL, then keep the remainder protected by the armed breakeven-trail (survival-first, not a loose free-roll). Full-exit always wins over a partial (manage-loop `elif`). **Partial-sell `ClosedTrade` accounting FIXED** (closes the LATENT backlog bug): `apply_sell` now accumulates `realized_cost/proceeds/pnl` per slice and books the FULL round-trip into ClosedTrade at close — so `trade_outcomes`/PnL stats are correct for partially-exited trades (a single full sell stays byte-identical). 93 tests; **1-agent adversarial review** confirmed accounting exact + race/guard/ordering correct → 2 wording/alert fixes. Live-verified earlier: a be_trail HOLD exit banked +0.0322 SOL. (Scenario sim does not yet model partials — live-observed + unit-tested instead; optional follow-up.)
- [~] P4 — **In-distribution model** (PLUMBING DONE; actual training still DATA-GATED — labelable_5m_indexed≈15-17, need ~hundreds). Prep shipped + adversarially reviewed (no HIGH/MED bugs):
  - ✅ `retrain.py --population observations --indexed-only --death-drop 0.9` — trains on the UNBIASED in-distribution set (liquidity>0, death-aware labels). `indexed_only` auto-on for the observations population; legacy `candidates` path byte-preserved.
  - ✅ **Precision-over-base-rate deploy gate** (`should_deploy` + pure `precision_over_base`): deploys only if forward-val AUC ≥ max(floor, deployed) AND operating-threshold precision beats the positive base rate by `--min-precision-lift` (0.05) on ≥ `--min-pred-pos` (10) predicted-positives — AUC alone hides a useless entry point on a rare-event imbalanced problem. `_train` returns val precision at the live `gbm_entry_threshold` (same held-out rows as AUC, no leakage). Walk-forward time-split retained. NEVER deploys a worse model.
  - ✅ **Shadow mode** (`gbm_shadow_model_path`, off by default): `Bot._features_json` records a candidate model's probability into each observation's `shadow_score` (a non-FEATURE_NAMES key, ignored by training/scoring/archival) so a fresh model is validated against REAL forward outcomes BEFORE it is promoted to size trades. Verified: true no-op when off; NEVER influences a live decision; no crash if the model fails to load.
  - ⏳ STILL TODO (do only when data is sufficient): run the actual train/deploy on the in-distribution set; add trajectory features (m5/h1/off_high/trend/ema) to FEATURE_NAMES (invalidates the old model → retrain). Do NOT train on ~15 mints. 102 tests; precision_over_base + precision-gate + plumbing unit-tested.

### Phase R2 — Researched strategy additions (deep-research workflow wgm79rv3n: 106 agents, 24 sources, 18/25 claims verified)
**Honest meta-finding:** on free data there is NO new "alpha" — the edge is AVOIDANCE/discrimination, which we've largely built. The study strongly CORROBORATED our existing design: survival-first is correct (98.6% of tokens collapse, only 0.6–2% graduate); volume/market-activity is the #1 signal (academic ablation, matches our vol_h1); buy/sell ratio is NOT predictive (high turnover ⇒ LOWER graduation); chasing late big movers is a TRAP (82.8% of >100% gainers are manipulated → our anti-blow-off/far-off-high vetoes validated).
Prioritized, evidence-based, FREE-tier additions:
- [x] R2-a — **VOLUME now leads the scorer** (#1 corroborated signal): added `s_vol_h1=_norm(vol_h1, min_vol_h1*2=3000)` to `score_candidate`; momentum reweighted to `0.30·vol_h1 + 0.20·volspike + 0.20·vol/mcap + 0.20·buyers + 0.10·buy_sell` (buy/sell cut 0.25→0.10, non-predictive per the study). RE-DERIVED `rule_score_ref_hi` 0.62→**0.70** from the new raw distribution (129k candidates): entry 0.50 = top ~12% (1824 mints, active), slam-dunk 0.85 stays rare (~53). 86 tests; unit-tested (vol_h1 lifts score). Bot restarted.
- [x] R2-b — **Graded holder-concentration penalty** (study: ~half of manipulated tokens have top holders >30%): `scam_likelihood` now adds +0.10 when top5 concentration > `concentration_warn_pct`=30 (and +0.20 at >90, the extreme). Was only flagging >90 (too lax). Unit-tested. (Kept top-5 read — extending to top-20 needs re-tuning holder_cut/good thresholds; noted as future.)
- [x] R2-c — **"Revoked" is necessary, not sufficient** (attackers renounce then dump behaviorally): the candidate prompt now states revocation passed the check BUT to also weigh behavioral signals — without weakening the R14 polarity clarity (which fixed gemma's misread). Demo confirms gemma weighs authorities + concentration + its own loss-streak together.
- [ ] R2-d (low) — Free graduation/migration awareness via `getAccountInfo` bonding-curve `complete` flag — but marginal here (DexScreener visibility ≈ graduation for us).
- [ ] R2-e (research-only) — Smart-money/proven-wallet tracking: infra is free (Helius webhooks 5×100k) but EDGE IS UNPROVEN — build a pipeline to research, don't assume alpha.
- **AVOID (refuted in verification):** wash-trade detection from vol/price mismatch (refuted), a buy/sell-ratio signal (non-predictive), chasing big movers, "liquidity-velocity is the top predictor" (refuted), and banking any specific edge magnitude (the 34pp / F1-0.96 claims were refuted). **No verified free-data edge** was found for market-regime/risk-off, correlation, time-of-day, sentiment, mean-reversion, or MEV — treat as folklore until shown otherwise.

### Phase W — More demo VOLUME + higher WINRATE (user 2026-06-02: "do many demo trades, collect good data, raise the win rate")
Goal: increase paper-trade VOLUME (more trades -> faster in-distribution data accrual -> unblocks the data-gated P4 GBM) WITHOUT degrading the win rate, all GROUNDED in the collected data. Funnel finding: ~5792 buy-verdicts -> only ~78 executed trades (~98.6% blocked in `_try_open`); real trades n=7 are ALL HOLD at 86% win (+0.716 SOL). So volume — not winrate — is the binding gap, and the lever is the verdict->trade funnel, not loosening selection.
- [x] W1 — **Diagnose + design** (8-agent workflow). KEY HONEST FINDING that overturned the optimistic headline: the `trade_outcomes` table (7 rows) was a RECENCY-biased subsample; reconstructing the FULL `trades` table (74 mints / 38.5h) shows the bot is **net-NEGATIVE** — SCALP 0/39 wins (~-0.7..-0.9 SOL, round-trip 9.5% never cleared) and HOLD net-negative because losses are RUGS that gap past the -35% price SL at ~3% slippage (realized -90%+). Entry SCORE and entry LIQUIDITY both fail to separate winners from rugs (fully overlapping), so the fix is on the EXIT side, not a stricter entry. Volume bottleneck is SUPPLY of DexScreener-indexed mints (~43 distinct in 209k candidates; 99.9% curve-only), NOT the downstream gates — so lowering entry_threshold adds ZERO tradeable mints. **Always reconstruct PnL from `trades`, not `trade_outcomes`.**
- [x] W2 — **Applied the data-grounded changes** (config-favored, all reversible/paper-safe; 105 tests; 12-agent adversarial review → 5 confirmed fixes incl. 1 HIGH). Live (bot b6pnmsxiz): `scalp_buys=False | max_positions=5 | exposure 0.20 | liq_collapse<50% | blackout=20`.
  - **SCALP buy lane OFF** (`trade_scalp=False`): 0/39 wins = pure bleed. Vetoed on the INTRINSIC `c.mode` (not the LLM-overridable `verdict.mode` — review HIGH: the brain could relabel a curve scalp as "hold" and bypass). Scalp candidates still scored + observed (P4 data kept).
  - **Liquidity-collapse exit** (`ExitParams.liq_collapse_frac=0.5`): force-close a HOLD when its pool drains below 50% of ENTRY liquidity (or sub-floor) — the rug tell the price SL gaps past. Checked in the new `_observe_held` each eval cycle (snapshots already in hand). Pure `_liq_collapsed` unit-tested.
  - **Observe HELD positions** (`_observe_held`): held mints (excluded from the buy path) are now snapshotted + logged as observations so their 5m P4 label completes (avg HOLD ~183s < 300s horizon — they used to drop out exactly when the label forms). `_candidate_from_snapshot` extracted (shared, unit-tested) for the ranking + held paths.
  - **More HOLD concurrency**: max_positions 3→5 + max_total_exposure_frac 0.15→0.20 (mtef is the TRUE concurrency governor; max_positions alone was inert). Per-trade rug still 5%-capped; whole-book rug 20%. **daily_loss_cap KEPT at 1.5** (deliberate survival-first deviation from the synthesis's 2.0; the reviewer confirmed 1.5 is the safer side of the gap — the breaker trips earlier).
  - **Wider intake**: watch_limit 120→240, max_safety_checks_per_cycle 10→20, watch_max_age_s 1800→2700, faster blackout (max_price_misses 60→20). Grows the true SUPPLY ceiling + P4 data, no gate change.
  - **Rejected (data-refuted):** raising setup_min_liquidity_usd (winners/rugs overlap), raising entry_threshold (score non-separating + supply-bound), tightening hold_sl (rugs gap past it), turning on the LLM (adds skips, costs API).
  - ✅ Follow-up done: `_observe_held` async integration test (closes on liq_collapse, observes a healthy held mint) + `_candidate_from_snapshot` field-mapping test. 106 tests.
  - 📊 **MEASURED (first ~6h post-W2, reconstructed from `trades` via the equity-reset boundary — NOT trade_outcomes):** 15 buys / **14 closed, all HOLD, net +0.5274 SOL (+5.3%), win% 43%, profit-factor 1.92**, worst −0.168 / best +0.638; **scalp_buys=0** (bleed eliminated), partial_tp fired 7×. Flipped the bot from net-NEGATIVE (pre-W2) to net-POSITIVE. Mid-session it peaked at **+8.6% / pf 4.5 / 58% win** before a late cluster of rugs pulled it back. **Honest caveats:** n=14 thin; +5.3% leans on one +0.638 winner.
- [x] W3 — **Win-rate root-cause: the rug cohort + the concentration blind spot.** Per-trade analysis of the 14 closed: the 3 catastrophic SL losses (−40%, −58%, −70%) are the ENTIRE drag (~−0.41 SOL); the 4 'fade' exits were small/protective (−0.1..−9.9%); wins came from be_trail + one +237% tp. Compared the rugs' vs winners' ENTRY features — score, liquidity ($11–15k vs $14–32k), bsr, mcap all OVERLAP (no separation; bsr is study-refuted anyway). The ONE tell that could separate a whale-rug — **top-5 holder concentration — is 0/2000 KNOWN (always −1)**. Root cause found + verified live: the configured RPC is the **PUBLIC `api.mainnet-beta.solana.com`**, which serves `getTokenSupply` but returns EMPTY for `getTokenLargestAccounts` → `top5_concentration` is always None → the R2-b scam penalty, holder-quality lean, and R13 re-eval are all DARK. The "helius=True" startup log was misleading (it only meant the URL string was non-empty). **W3 fix (autonomous part):** added `HeliusRPC.supports_largest_accounts()` + a startup probe that honestly logs "holder-concentration safety ENABLED/DISABLED" (no key leak); 107 tests. **The #1 win-rate lever is now a USER action (FREE, unlike G3):** set `HELIUS_RPC_URL` to a free Helius key → activates the concentration scam-tell that targets the exact rug cohort causing every catastrophic loss. Until then, free-tier price-dump rugs are structurally hard to dodge (the −35% price SL gaps past them at ~3% slippage; the liquidity-collapse exit only catches LP-PULL rugs, not sell-offs). Did NOT tighten the SL or add refuted entry filters (W1-refuted, n=3 too thin). Bot NOT restarted (W3 is startup-log-only, zero trading-behavior change — keep accruing the session + data).
- [x] W4 — **Helius key wired → concentration LIVE + entry whale-rug VETO** (user supplied a free Helius API key 2026-06-02). Created `.gitignore` (covers `.env`, verified via `git check-ignore`) BEFORE writing the key to `.env` (`HELIUS_RPC_URL=…helius-rpc.com/?api-key=…`; config already loads dotenv). Verified live: `top5_concentration` returns real values (e.g. **97.6%** on a recent indexed mint = a whale-rug). Fixed the W3 probe — the live mint-probe false-negatives (getTokenLargestAccounts returns EMPTY for huge mints USDC/WSOL/BONK even on a working RPC), so `supports_largest_accounts()` is now a URL heuristic (public mainnet-beta → dark; enhanced RPC → live). Startup now logs **"holder-concentration safety ENABLED"**. **New entry veto** (`Bot._concentration_vetoed`): skip a BUY when top-5 (non-vault) concentration is KNOWN and > `max_top5_concentration_pct`=90 (the extreme whale-rug line) — unknown → no veto (never block on missing data; dataset/observation unaffected, so it's a BUY-only veto like the scalp veto). Broadened the concentration FETCH from `st.migrated` to any indexed token (liquidity>0 == a real pool == off-curve) so the veto covers every tradeable HOLD. The R2-b graded penalty + holder_quality + R13 re-eval now run on REAL data automatically.
  - 🔬 **MEASURED the live concentration distribution (the moment it turned on) — and it forced an honest correction.** Fresh pump.fun graduated tokens are **UNIFORMLY high-concentration**: 16 live top-5 (non-vault) values ran **82–96%, median ~89, none ≤80**. So static top-5 concentration does **NOT** cleanly separate rugs from winners (they're all concentrated post-graduation). Worse, this EXPOSED that every concentration threshold was mis-calibrated for a "normal 0–90" range and would now HARM: `holder_cut_pct`=85 (R13 cut) sat BELOW the median → R13 would dump ~81% of held winners; the entry veto + `max_top5`=90 would block ~38%; `holder_good_pct`=40 was unreachable (R13 never "extend"); `concentration_warn_pct`=30 fired on everything. **W4 recalibration (data-grounded, reversible):** re-centered onto the live distribution — `max_top5_concentration_pct` 90→**96** (veto + R2-b +0.20 only at near-total dev control), `concentration_warn_pct` 30→**90**, `holder_cut_pct` 85→**96**, `holder_good_pct` 40→**85** (dataclass + load + tests updated). This is DEFENSIVE (stops the thresholds from cutting/blocking the median winner); it does NOT claim concentration now separates rugs. 108 tests; live (bot bqzkwa1bt). **HONEST STANCE:** the W3/W4 hypothesis ("concentration is THE win-rate lever") is only PARTIALLY borne out — concentration is now visible/logged but not obviously discriminative as a static filter; whether the extreme tail (>96) or the concentration TREND (rising-while-held = the R13 idea) actually separates rugs needs LABELED concentration+outcome data, now accruing. n=16 thin — re-tune from a larger labeled sample. Did NOT over-commit to a static veto on unvalidated separation.
  - 🛠️ **Built `backtest/concentration_study.py`** (the rigorous, repeatable test): joins each CLOSED trade's REAL entry concentration (`trade_outcomes.entry_features`) to its realized PnL and reports winner-vs-loser concentration separation + an honest verdict (INCONCLUSIVE until ≥5 of each class; SEPARATES vs NO-separation otherwise). Pure `concentration_split`/`verdict` unit-tested (109 tests). Current live verdict: **INCONCLUSIVE (1W/0L** with known entry concentration — concentration only went live ~12:38, few closes since; intriguingly that 1 winner entered at just **19.3%**, well below the 82-96% indexed-observation band, hinting TRADED winners may skew low-concentration — but n=1). Run it as labeled trades accrue; only then re-tune the veto/R13 thresholds toward any clean winner/loser band.
- [x] W5 — **Creator rug-history filter** (user's #1 pick — behavioral, free, the strongest untapped anti-rug lever). The `tokens.creator` wallet is 100% captured; 2854 creators launched >1 token, the worst spamming **600-726** — a wallet that spam-launches hundreds of tokens is a serial-rug/pump factory, not a legit dev. New `data/creator_history.py` (CreatorHistory: seeded once from `storage.creator_launch_counts()` at startup, incremented per NewTokenEvent in `_ingest_loop`, O(1) `launch_count`); `_candidate_from_snapshot` sets `c.features["creator_launches"]`; `_features_json` logs it (non-FEATURE_NAMES, for later study); the brain prompt shows it. **SOFT/graded, NOT a hard veto** — live data showed launch count is real-but-NOISY: our worst rugs came from 65/67-launch creators BUT the +237% winner came from a 20-launch creator, and low-launch (1-2) rugs exist; and ≥10-launch creators are 61% of supply, so a hard veto would block winners + starve the thin supply. So `scam_likelihood` adds **+0.15 when creator_launches > `creator_spam_launches`=40** (catches the 65/67 spammers, spares the 20-launch winner); the brain weighs it. 110 tests; live (bot bpogaxyw0). ⏳ FOLLOW-UP: a creator-launches-vs-outcome study (like `concentration_study`) to calibrate the threshold from labeled trades; later, prior-token SURVIVAL/graduation rate per creator (richer than raw count) + LP-lock verification.
- [x] W6 — **Concentration-TREND rug-cut + EV/Kelly math skeleton** (user picked the remaining 3 directions). (1) **Concentration-trend cut** (`Bot._concentration_rose` in `_reeval_loop`): static concentration is uniformly high (didn't separate), but a RISE while held = dev/insiders consolidating = distribution prep = rug imminent — cut "conc_rise" when current top-5 climbs ≥ `conc_rise_cut_pct`=8pp above ENTRY (from `pos.entry_features`). Uses the now-live concentration data; complements R13's absolute-level cut. (2) **EV + fractional-Kelly sizing skeleton** (`risk.limits.kelly_fraction` + `use_kelly_sizing`/`kelly_fraction_mult` config, wired into `_try_open`): edge-proportional sizing `f* = p − (1−p)/b` (b=tp/sl), half-Kelly, 0 on non-positive edge; the per-trade 5% + exposure caps still bind on top. **OFF by default** — Kelly on a miscalibrated `p` amplifies losses, so it's gated until P4 supplies a calibrated win-probability; the skeleton is ready to flip on with a proven edge. Pure `kelly_fraction` + `_concentration_rose` unit-tested. 112 tests; live (bot bam8gjekh). (3) **P4 still data-gated but ACCELERATING** — labelable_5m_indexed jumped **17 → 77** (W4 observe-held + broadened fetch + wider intake working); at ~200 it becomes trainable (the calibrated-p that powers EV+Kelly). Did NOT train on 77.

### Phase N — Next-steps roadmap (2026-06-03; 5-lens, code-grounded, adversarially-critiqued — 11-agent workflow wf_3f92dd62)
**Framing (anchored to RESEARCH.md):** loss-minimisation + honest-measurement program, NOT profit. The binding constraint on the whole system is **labelable-mint count + validation quality**, not cleverer trading. Nothing below assumes an edge. The highest-leverage work is DATA + MEASUREMENT, not smarter entries (~129 labelable vs GBM's ~200; separation only 32/30 winner/rug; the "scale out FAST" doctrine is exactly what the only offline validator — exitlab — is BLIND to). Execution order: **#N1 → (#N3 + #N8) → #N2 → (#N4/#N5) → #N7**, then the rest.
- [x] **N1 (measure, M) — Lift the forward-TRACKING ceiling** *(DONE 2026-06-03)*: new `_track_loop`/`_track_once` (main.py) re-prices observed-but-not-bought INDEXED mints from the DB (storage `tracking_candidates`, `revisit_s` guard avoids double-logging active mints) until `track_window_s`, logging a minimal non-empty (liquidity-bearing) forward observation each pass — labelers use each mint's EARLIEST obs for features so a tracking row only adds the later PRICE (no train/serve skew). `track_*` knobs in config; `tracking_stats` + a status funnel line. Bounded (≤1 DexScreener batch/pass), off the hot path. Live `status`: **133 indexed mints, 100 labelable@5m** (need ~200 for the GBM) — the bottleneck is now measured and rising. 3 tests.
- [ ] **N2 (exit, M) — exitlab models the FULL scale-out ladder** (partial TP + proactive price-only derisk), not just `should_exit`. `replay_exit` walks only `should_exit` ("partials excluded") yet the live manage loop fires `should_take_partial` + `_derisk_if_profitable` → the user's #1 doctrine is unvalidated folklore today. Meta-enabler for N4/N5. `apply_sell` already books multi-leg PnL. Caveat: `load_paths` discards per-tick concentration, so the RISK-gated derisk can't be replayed (only the price-only one) until the dataset logs per-tick concentration.
- [ ] **N3 (survival, S) — Rolling realized-PnL halt** replacing the reset-on-any-win consecutive-loss breaker. `on_win` (limits.py:165) resets `_consecutive_losses` on ANY non-neg close → alternating {small win, big loss} bleed never trips it; at ~24% win-rate that's the NORMAL case. Halts new entries only; clear on day-rollover; calibrate the SOL/window from `trade_outcomes`.
- [ ] **N4 (exit, M) — Tiered (laddered) partial TP**, default unchanged at one tier. `partial_taken` is a latching bool → exactly ONE partial fires. Sequence AFTER N2. Design jointly with N5 (shared latch, avoid double-sell). Give-back MITIGATION, not an edge — more legs = more fee drag if they stall.
- [ ] **N5 (exit, S) — Proactive principal-recovery derisk** (bank principal once up ≥ `derisk_proactive_pct`, no risk flag required). Most literal encoding of the doctrine; machinery (`derisk_fraction` + `_derisk_if_profitable`) exists, only the price-only trigger is new — and IS replayable in the upgraded exitlab. Overlaps N4; set the threshold from exitlab, never by feel.
- [x] **N6 (measure, S) — Per-feature coverage report + dark-column guard** *(DONE 2026-06-03)*: `features.py` `feature_coverage`/`dark_features` (pure); `train_gbm.py` WARNS on dark/constant columns before training (does NOT drop — FEATURE_NAMES is load-bearing); new `python -m memebot.backtest.coverage` CLI. Live report (19,023 indexed rows) confirms **4 DARK columns**: `bundle_share`, `creator_dump_ratio`, `smart_money_share` (G3-tape, not yet accrued) + `lp_burned_pct` (never populated — LP-burn detection still deferred); trajectory features only ~10-24% covered. So a GBM trained today must not read those 4 importances as signal. 1 test.
- [ ] **N7 (copytrade, S) — Pin smart-money strictly advisory**: one CONSTITUTION/brain-prompt line in `agent/policy.py` forbidding wallet-following EXECUTION ("smart-money is a LEAN, never a trigger"). Direct following needs sub-second fill (co-located RPC, Jito) we lack; on free data we'd be exit liquidity. Negative-work leverage.
- [ ] **N8 (survival, S) — Auto-tighten size/exposure when holder-concentration is DARK** (public RPC) instead of trading blind at full size. `supports_largest_accounts()` already probed at startup; config-only `conc_dark_size_mult` into `size_frac`. Risk-reduction-by-shrinking; the real fix is just running a free enhanced Helius RPC.
- [x] **N9 (survival, M) — Correlated-cohort exposure cap** *(DONE 2026-06-03)*: `_try_open` blocks a buy once `max_creator_cohort`=2 open positions already share the candidate's creator wallet (`Bot._cohort_full`; `Candidate.creator`/`Position.creator` threaded via `_candidate_from_snapshot` + `apply_buy`). Unknown creator never capped; cap 0 = off. Treats N same-creator positions as ONE correlated bet (research: catastrophic losses are correlated flushes). 1 test.
- [x] **N10 (survival, M) — Daily kill-switch tracks intraday PEAK** *(DONE 2026-06-03)*: `Portfolio.peak_giveback` tracks the intraday high-water mark (reset on day rollover); `RiskManager.can_open` halts "giveback" when ≥ `daily_giveback_frac`=0.7 of the day's PEAK GAIN is given back, armed only after the gain cleared `giveback_arm_frac`=0.05 of day-start equity (so it can't strangle thin flow). Catches a winning day round-tripping — invisible to `daily_loss` (start-of-day baseline). Clears on rollover; getattr-guarded for stub-safety. LOOSE prior — calibrate from the equity series. 2 tests.
- [x] **N11 (measure, M) — Paper-vs-live FILL-DIVERGENCE harness** *(DONE 2026-06-03)*: `backtest/fill_divergence.py` re-reads each traded mint's logged observation price series and compares the decision-time price to the price `--polls` (default 2) observations later — the price a latency-delayed live fill would face. Reports per-side adverse divergence + the implied EXTRA round-trip cost vs the ~9.5% cushion. Live (151 fills, 2 polls): buy adverse mean +6.43%, sell side favorable −5.35%, **net +1.08% extra round-trip = ~11% of the cushion** (a WEAK LOWER BOUND — real MEV is worse; median buy −1.48% so the mean is fat-tail-driven). The honest instrument that gates any G1 conversation. 1 test.
- [x] **N12 (signal, L) — Dev-wallet funding-source clustering** *(DONE 2026-06-03)*: `helius_rpc.get_funder` resolves each creator's funding wallet (getSignaturesForAddress oldest → getTransaction source; standard methods, work on public RPC); `data/funder_history.py` `FunderRegistry` re-links rotated creators by common treasury; `storage` `creator_funders` table + `unresolved_creators`; `_funder_loop` resolves a bounded batch off the hot path, cached + persisted; `scam_likelihood` adds +0.15 when the funder spawned a `[funder_serial_min=3, funder_serial_max=200]` BAND of distinct creators — the band AUTO-EXCLUDES infrastructure (funds everyone) so NO hand-maintained denylist is needed. `funder_creator_count` is dataset-only (NOT in FEATURE_NAMES, like creator_launches); brain line added. Self-degrades without an RPC. UNPROVEN — calibrate the band from labeled outcomes. 4 tests.
- [x] **N13 (signal, M) — Buyer-breadth VELOCITY** *(DONE 2026-06-03)*: `token_state` `buyer_obs` + `record_buyers`/`buyer_trend` (change in the rolling-1h buy count across the polled window — accelerating vs fading breadth, the only NON-degenerate form since buys_h1 is an integral); `buyer_growth` APPENDED to FEATURE_NAMES (now 29), default **0.0** not −1 (the delta is SIGNED, matching trend_chg_pct/vol_per_min). Works in the DEFAULT no-stream config (free DexScreener). Advisory; velocity was REFUTED standalone → weak GBM contributor only. 1 test + roundtrip.

**🔬 P4 GBM diagnostic (2026-06-04) — the data crossed from NO-signal to REAL-but-non-deployable signal.** With the N1 track loop, in-distribution `labelable_5m_indexed` reached **126** (23 pos / 103 neg, 18%). Ran the honest deploy-gated retrain + a rigorous diagnostic:
  - **AUC 0.79** forward-val (was 0.53 in the old no-signal era) — and a **label-shuffle permutation test confirms it is REAL**: shuffled-label AUC collapses to mean **0.48** (≈ noise). So there IS a genuine winner-vs-loser pattern now.
  - **NOT deployed** — the precision-over-base-rate gate correctly REFUSED: at the entry threshold the model yields **0 actionable predicted-positives** on the ~28-row val split (AUC ranks well but gives no usable entry point yet). Rule scorer KEPT. This is the gate working exactly as designed (AUC alone would mislead on a rare-event imbalanced problem).
  - **Feature importance** is concentrated in the FULLY-COVERED columns (liquidity_usd, vol_to_mcap_pct, unique_buyers, buy_sell_ratio, vol_h1); **all 4 N6-flagged DARK columns scored exactly 0** (validating N6 end-to-end), and the new `buyer_growth` (N13) scored 0 too (velocity contributes nothing yet, as the research predicted).
  - **Honest verdict:** real progress (no-signal → real signal) but still NOT a deployable edge; and AUC 0.79 is offline + survivorship-shaped (labelable = survivors; death_drop 0.9 only partly mitigates) so it may not transfer live (the prior 0.75 model went 6/6 losing). Keep accruing via N1 until the val split is large enough for the precision gate to pass + the threshold to be re-derived. Shadow mode is the pre-promotion guard.
  - 🐛 **Found + fixed a real bug during this:** `train_gbm.py`'s N6 warning print used a `⚠`/`—` (non-ASCII) → `UnicodeEncodeError` crash on the Windows cp1252 console whenever dark columns exist (i.e. always). Now ASCII-only. 196 tests green.
  - ⚙️ **Pipeline fix — class-imbalance correction (`gbm_balance_classes`, `retrain.balanced_params`).** The "0 predicted-positives @ thr 0.4" was a textbook RARE-POSITIVE artifact: without a class prior the GBM's probabilities cluster near the 18% base rate, so it predicts ~no positives at the entry threshold. Added `scale_pos_weight = n_neg/n_pos` (a class-prior fix, NOT data-snooping — correct at any sample size) to BOTH training paths, on by default; pure `balanced_params` unit-tested. Re-ran the deploy-gated diagnostic: predicted-positives **0 → 9** at thr 0.4 (the model is now operationally shaped), AUC stable **0.792**. **Still NOT deployed** — precision **0.111 < base 0.077 + 0.05 lift** AND n_pred_pos 9 < 10, so the 9 entries are barely above chance. The deploy gate works perfectly; the verdict is unchanged (keep collecting) but now for the RIGHT reason (no precision EDGE yet, not a calibration artifact). The pipeline is now correctly configured for the imbalanced problem, so it's ready the moment the data clears the gate. 197 tests green.
  - 🔧 **Fixed the train/serve skew:** `retrain.py` now builds its training matrix via `row_from_feats` (serve-consistent FEATURE_DEFAULTS sentinels) instead of `f.get(name, 0.0)` — so a key missing from an old logged row encodes −1/−2, exactly as the live scorer sees it (the P2 discipline that retrain had missed). 197 tests green.
  - ⚠️ **HONEST REVERSAL — the AUC is NOT robust on thin data.** Re-running minutes later (the live track loop grew the in-distribution set **126 → 157** mints) with the serve-consistent encoding, the forward-val **AUC collapsed 0.79 → 0.48** (below chance on the new val fold; the deploy gate refused on AUC-below-floor). So the earlier 0.79 (even with its passing permutation test) was an artifact of ONE 28-row val split, NOT a stable signal — AUC swings wildly across data snapshots taken minutes apart. This STRENGTHENS the verdict: do NOT trust any single AUC reading on thin data; the multi-guard deploy gate (AUC floor + precision-over-base + min-pred-pos) is exactly what prevents shipping noise. Keep accruing until the estimate is stable AND clears the gate. The pipeline fixes (imbalance, encoding) are correct regardless; the data simply isn't there yet.
  - 🔒 **LP-lock detection (2026-06-04, RESEARCHED then built minimal) — honest LOW value.** A 5-agent web-research workflow established the current reality: since 2025-03 pump.fun graduates onto its own AMM **PumpSwap**, whose `migrate` instruction **BURNS the LP by protocol design** — so lp_burned ~= 100% for the ~0.6% that graduate (a CONSTANT, not a per-token signal), and the dominant rug is a TOKEN-dump (~92%, a concentration question), NOT an LP-pull. Per the spec's "don't build the full scanner" verdict, built only the FREE **Branch-1**: a pump-AMM graduate (`dex_id.startswith("pump")`) sets `lp_burned_pct = 100` with NO RPC, reviving the otherwise-DEAD gate (lp_burned was 0% populated → the gate silently no-op'd) + removing false comfort. Deferred the complex/marginal **Branch-2** (non-pump 'pullable' pool LP-mint byte-parsing) — rare + needs getTokenLargestAccounts (public-RPC-dark) for ~0 discriminative AUC. Advisory/None-tri-state honest; not a new hard veto. 211 tests. **Honest takeaway: the user's "did the source lock its coin?" instinct is sound in general, but for pump.fun the protocol handles it — the real rug lever stays top5_concentration (esp. RISE-while-held) + the G3-gated coordination signals.**
  - 🛡️ **Rug-VETO dodge trade-off (2026-06-04, `rug_model.dodge_tradeoff`) — the honest answer to "can we dodge 60-90% of rugs like the pros?": on our signals, NO.** Out-of-fold P(rug) cost curve (50 rugs / 39 winners): to dodge **60% of rugs (thr 0.3) we'd lose 54% of winners**; at EVERY threshold winners_lost ≈ rugs_dodged (they move together — the veto is near-random, consistent with rug-AUC ~0.55-0.62). So wiring a rug-veto on CURRENT free-tier signals would cut winners ~as fast as rugs = no net help. The pros' 60-90% dodge uses signals/speed we LACK (bundle/coordinated-wallet detection, smart-money tape, social, manual judgment, faster info). Honest steer: the path to real rug-dodging is the MISSING SIGNALS (G3 tape for bundle/smart-money — spends SOL) + more data to firm the separation, NOT a veto on today's data. Pure `dodge_tradeoff` unit-tested. 211 tests.
  - 🧩 **Orchestration layer + setup-aware learning loop (2026-06-04) — the "workflow classification / AGI-like" piece.** `signals/setup.py` `setup_type()` classifies each candidate's SITUATION (the playbook): **creator_gold / safe_hold / momentum / risky / unproven** (survival-first ordering, riskiest wins), fed to the brain with a per-type playbook line + logged (`c.features["setup_type"]`, dataset-only). Closed the LEARNING LOOP: `setup_type` threads onto Position→ClosedTrade (field-copy, no PnL math touched), reflection tags the lesson with the setup (`schema.setup_tag`: creator_gold→gold etc.; `_ALLOWED_TAGS` extended), and `memory.recall` now also matches the candidate's setup_type — so a 'creator_gold' loss is recalled for future creator_gold candidates (classify → decide → reflect → recall). Plus a TIMING note in the CONSTITUTION (measured: winners take several minutes to peak, rugs strike early). 210 tests. Honest framing: this improves decision STRUCTURE + reasoning consistency + setup-specific learning — it does NOT manufacture signal (the data ceiling holds).
  - 🪤 **Front-run-the-rug idea — DATA-TESTED (user's idea): real edge location, but infra-gated.** The user proposed sniping a launch tiny + selling BEFORE the rug. Tested on logged paths: **57% of rugs DO peak above the 9.5% round-trip** (median +14%), and PERFECT peak-timing nets +5% (rugs) / +16% (all). BUT the REALISTIC exit nets **−95% (rug) / −15% (all)** — the ~100pp gap is ALL execution SPEED. Capturing the peak needs sub-second detect→buy→sell + curve pricing (G3, spends SOL) + selling before the dev/snipers (Jito/co-located RPC) — the exact MEV infra we lack. The clearest evidence yet that the ceiling is an INFRASTRUCTURE problem, not a strategy one: the money is real (pre-rug peak), but un-capturable on free data + retail latency. Honest staged conclusion (user's): accrue data now (free, post-graduation), scale when infra catches up.
  - 🤖 **Autonomy supervisor (2026-06-04, `memebot/supervisor.py`) — "if my computer is on, the AI runs by itself".** The bot already runs autonomously via its asyncio loops *once started*; the missing piece was RESILIENCE. `python -m memebot.supervisor` runs `python -m memebot` as a child and auto-RESTARTS it on any unexpected exit (unhandled exception / network cascade / OS hiccup), with exponential backoff + a fast-crash cap (gives up rather than spinning on a deterministic config error); a healthy run resets the backoff. Clean exit-0 = intentional stop (not restarted); Ctrl-C or a `memebot.stop` file stops everything; the bot's single-instance lock prevents overlap. Run-on-BOOT is left as a deliberate user-side OS step (Task Scheduler) the script does NOT take. Pure `_next_backoff`/`_should_give_up` unit-tested; graceful-stop path smoke-verified live. PAPER-ONLY — only keeps the paper bot alive, changes nothing about execution mode. 208 tests. Documented in CLAUDE.md.
  - 🧠 **Self-curating lesson memory (2026-06-04) — the "AI owns + improves its own knowledge" piece.** `agent/memory.py` rewritten: on load (and incrementally on store) it DE-DUPLICATES near-identical lessons into ONE entry with a hit-count (REINFORCEMENT — a lesson independently re-learned is more trustworthy), scores `(confidence + reinforce_bonus) * age_decay` (~14d half-life), and `recall()` now surfaces the highest-SCORE (most-validated) tag-matching lessons, not merely the newest; overflow drops the LOWEST-score entry (never a hard-delete of a reinforced pattern). New `storage.lessons_full()`. Live proof: the bot booted "loaded **116 lessons (curated from 167 raw)**" — ~31% were duplicates, now merged + reinforcement-weighted. Makes the memory HONEST + self-improving; does not by itself raise the ceiling. Pure `curate_lessons`/`_norm` + store-reinforcement + recall-by-score unit-tested (207 tests). NOTE on LP-lock: deferred pending research — pump.fun graduates have their LP auto-handled by the protocol (often burned), so the check is narrow/confirmatory there + only ~0.5-2% graduate; build it right (current PumpSwap mechanics) rather than brittle.
  - 🧑‍🎤 **User-vision builds (2026-06-04) — creator-source as the dump-avoidance lever.** After an honest concession (the user is right that better SELECTION can raise win-rate ON the selective subset + that creator-SOURCE quality is the strongest rug-avoidance axis — I'd overstated "never raises win-rate"; the honest nuance is fewer trades + EV still fee-capped, so still loss-MIN not sustained profit). Built two of their precise insights: (1) **CREATOR-SOLD CONDITIONAL** — `scam_likelihood` no longer penalizes a creator-dumped token that is THRIVING (polled trend rising OR price_change_h1>0): creator-out + still-rising = the dev overhang is GONE + organic demand = a GOLD setup, not a scam. Tightened to SUSTAINED strength (not a single m5 tick) so a pump-then-rug can't escape; defaults to the penalty on unknown/flat. Aligns the scoring with the constitution's long-standing "creator garsan ch ... trendline" nuance. (2) **DUPLICATION tell** (`data/metadata_history.py` MetadataRegistry) — a name+symbol reused across > `name_reuse_warn`=6 mints = a scam-factory reusing branding (a fresh creator/funder can't hide reused identity); seeded from the tokens table, recorded per NewTokenEvent, a soft +0.10 advisory scam term (popular memes repeat legitimately → noisy → never a veto, calibrate from data). `name_reuse_count` is dataset-only (NOT in FEATURE_NAMES). 205 tests; both unit-tested. NEXT agreed builds: LP-burn/liquidity-lock detection (the standout dead-code gap) + a lesson-validation loop (make the self-improving memory honest).
  - 🕒 **Rug-TIMING study (`backtest/rug_timing.py`) — clean NEGATIVE: a time-stop does NOT dodge rugs.** Hypothesis: if rugs cluster at a predictable age, a HOLD time-stop just before it dodges them. Measured (in-distribution): **time-to-RUG median 223s** (p25 72, p75 557) vs **time-to-WINNER-peak median 406s** — winners peak LATER than rugs collapse, the OPPOSITE of what a time-stop needs. So at every stop `winners_cut > rugs_dodged` (net −0.04…−0.32 across 60–1200s); the "best" (1200s) is barely-negative only because it touches almost nothing. Verdict: you can't exit your way out of rugs (they're early + overlap the winners' climb), which REINFORCES that rug-avoidance must happen at ENTRY (the rug-classifier veto / concentration), not via exit timing — consistent with the exitlab "partials/tight exits HURT" finding. The study did its job: a plausible lever ruled out by data. Pure `mint_timing`/`timestop_sweep` unit-tested. 202 tests green.
  - 💡 **NEW IDEA — rug-avoidance classifier (`backtest/rug_model.py`): flip the ML target to the DOCTRINE.** The project only ever trained WINNER classifiers (rare, NOISE). But the goal is rug-AVOIDANCE, and a rug may carry structural tells a winner doesn't. So predict the CATASTROPHIC outcome (rug/dead) and measure it HEAD-TO-HEAD with the winner classifier over the same stability folds. If it separates better, deploy it as a VETO (skip when P(rug) high — asymmetric + survival-first: only ever skips, never forces a buy). **Live, sharpening as data accrues: at n=191 RUG mean AUC 0.624 ± 0.075 → STABLE_EDGE vs WINNER 0.547 ± 0.126 → NOISE** (a fold below chance). So the novel thesis is now BORNE OUT — predicting the RUG is more learnable + more stable than predicting the winner (gap ~0.08 AUC); the rug is the project's strongest stable separation yet. **Rug tells (importance): vol_to_mcap, unique_buyers, market_cap, buy_sell_ratio, vol_h1, liquidity** — the MARKET-ACTIVITY profile, NOT concentration/creator (those are still too thin/dark to contribute). Still modest (0.624) + thin (~38 val/fold, folds 0.50-0.71) so NOT yet wireable; next disciplined step = a SHADOW rug-veto (measure forward before any live veto). Pure `rug_label`/`win_label` unit-tested. (At n=183 it was 0.550 vs 0.591; the rug edge is firming as labelable grows.)
  - 📈 **Readiness MONITOR (option 2) — durable go-live trajectory.** A bot-internal hourly `_readiness_loop` snapshots the gate metrics (CLEAN net/pf/win/n + labelable) to a `readiness_log` table (cheap, no GBM, off the hot path); `python -m memebot.readiness` now prints the trajectory so you can watch the book trend toward (or away from) net-positive across days. `save_readiness`/`readiness_history` unit-tested (rowid tiebreaks tied timestamps). Live (bot bld7n696s).
  - 🚦 **Built `python -m memebot.readiness` — the single auditable GO/NO-GO for real money.** Consolidates every honest instrument so a lucky number can't green-light live. The GATE is the CLEAN realized book: ≥ `go_live_min_trades`=100 closed, NET-POSITIVE, pf ≥ `go_live_min_profit_factor`=1.3 (config). Supporting (advisory): the GBM stability verdict, fill-divergence lower bound, labelable count. Pure `evaluate_gate` unit-tested. **Live verdict: NO-GO** — [PASS] 122 trades, but [FAIL] net −1.567 SOL & [FAIL] pf 0.67; supporting: labelable 145, stability NOISE, fill-div ~neutral. The expected honest result (ceiling = loss-minimisation); the report makes WHY explicit. Read-only; NEVER flips live mode. 199 tests green.
  - 🧪 **Built `backtest/gbm_stability.py` — the rigorous replacement for trust-one-AUC.** Runs K EXPANDING-WINDOW forward folds (train on a growing time-prefix, validate on the next block; no look-ahead) and reports the AUC DISTRIBUTION + a verdict (STABLE_EDGE: mean ≥ edge AND worst fold ≥ 0.5; NOISE: mean ≤ 0.55; else INCONCLUSIVE). Pure `fold_cuts`/`summarize_stability` unit-tested; shares the class-prior correction + precision gate. **Live verdict (n=176, 5 folds): NOISE** — per-fold AUCs **0.50, 0.67, (skip), 0.51, 0.51 → mean 0.545 ± 0.071**, only 25% of folds ≥ 0.55. So the 0.79 was a lucky split; across time folds the edge is **indistinguishable from chance**. This is now the honest readiness instrument — run it as data accrues; only a STABLE_EDGE verdict + a passing deploy gate justifies promotion. 198 tests green.

**AVOID (false hope / constraint-violating):** (a) `buy_sell_ratio(60)` into the 2s manage loop — `st.trades` is metered-G3-only, returns 0.0 in paper = dead code; (b) reconstruct bundle/sniper/smart_money from free DexScreener deltas — semantic contamination of the tape columns (salvage only as a NEW `dev_first_buy_frac`, never aliased); (c) direct follow-specific-wallets execution — G1-class, infra we lack; (d) velocity-scaled trail — velocity refuted + unreplayable offline; (e) pre-entry concentration DELTA — RPC-gated + starves the post-buy reeval budget; (f) wallet tiering over an empty ledger — premature (the wash-trade POISONING GUARD is the defensible half); (g) graduation-watch standalone — ~92% of graduates still dump; (h) lesson-memory de-dup/decay — lowest survival leverage.

**Infra reality (blunt):** free-data pump.fun trading CANNOT turn positive without speed/MEV infra; this roadmap does not change that (by design). The single highest-value "infra" action is not new infra — run a **free enhanced Helius RPC** to keep the holder-concentration layer LIVE. Honest ceiling stays loss-minimisation. **Do not promise profit.**

### Production-harden (2026-06-04) — G3 dead-stream diagnosed + handled gracefully
- 🔌 **G3 was ENABLED but ingesting 0 trade events (dark) — root cause FOUND + handled.** `.env` has `PUMPPORTAL_API_KEY` + `TRADE_STREAM_ENABLED=true`, so G3 was on, but the WS watchdog showed 0 events over 20 watched tokens. A temporary raw-message diagnostic surfaced PumpPortal's exact reply: **`'subscribeTokenTrade' is only available when connecting with an API key funded with at least 0.02 SOL`** + `Minimum balance not met` — so the api-key's PumpPortal wallet (`ExBsAB…Jt62`) is UNFUNDED. Not a code bug; the free new-token/migration subs work, only the METERED trade sub is refused. **Hardened:** `pumpportal_ws._is_trade_sub_rejected` detects the unfunded-key rejection, sets `trade_sub_unavailable`, clears the watchlist + stops re-subscribing (no spam / no churn), and logs the exact fix; `WatchlistManager.run` stops cleanly; a `_report_loop` dead-stream watchdog catches any other silent-G3 case. Verified live (the warnings fire on boot). 212 tests. **To ACTUALLY enable G3: fund wallet `ExBsABHSi4ZtYemtcNhYYpJUS2SC8TakwcAX173EJt62` with ≥0.02 SOL (user action — real SOL), then restart.** Until then G3 stays cleanly off and the bundle/smart-money/creator-dump tape signals remain dark (the dodge-tradeoff finding: those are the missing signals — so funding G3 is the one lever that could firm the rug-separation).

## Gated — NOT autonomous (require explicit user approval; do not start)
- [ ] G1 — Phase 5 live execution (Jupiter / PumpPortal Local Tx, self-custody) — **REAL MONEY / irreversible**
- [ ] G2 — Any remote push / deploy / publish / outward-facing action
- [~] G3 — Metered PumpPortal trade stream — **USER APPROVED then DEFERRED (2026-05-31).** Verified cost via docs: **0.01 SOL per 10,000 events + requires a funded api-key (≥0.02 SOL wallet)**. WIRED + ready: `PumpPortalFeed` attaches `?api-key=` to the WS URL (needed for metered subs), redacts the key from error logs, attaches it only when `trade_stream_enabled`; watchlist bounded (size 20) to cap spend. **DECISION: stay on the PAPER/demo account + FREE feeds until the system is fully built and the edge is PROVEN on free data — only then enable G3** (one env change: funded `PUMPPORTAL_API_KEY` + `TRADE_STREAM_ENABLED=true`). Do NOT auto-enable or re-ask. Trades stay PAPER regardless; real-money execution is G1 (still gated, NOT approved).

## Autonomous policy
- Each iteration: implement one item + unit tests + full `run_tests.py` + live smoke + check off.
- Milestone adversarial-review workflow after every ~3 completed code items (next: after R4).
- Cadence ~120s between iterations; if a review workflow is running, use a long (~1200s) fallback and let its notification drive the next step.
- **Done: R1–R8 + R10–R14 ✅ — the entire SAFE autonomous queue is complete.** 55 tests green; FIVE milestone adversarial-review workflows (Phase-1, brain-layer, R2–R4, R5–R7, R10–R14) → 22 confirmed fixes applied. Loop STOPPED here. Remaining: R9 (GBM trained on 171 samples but AUC 0.53 / no edge — kept rule scorer; collector still running, retrain when much more data accrues) and G1–G3 (live execution / metered stream / remote — require explicit user approval). Awaiting user direction.

## Notes
- Claude is advisory; the deterministic risk layer always vetoes.
- Default brain = local Ollama (no API key). Cloud Claude via `LLM_BACKEND=cloud`.
- Rule scorer runs until a GBM model is trained (R9).
